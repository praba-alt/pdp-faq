# pdp-faq

Generate user-focused FAQs from product pages given URL documents (CSV/JSON/TXT).

## Getting Started

1. Install dependencies:
   ```bash
   npm install
   ```
2. Build the project:
   ```bash
   npm run build
   ```

## Usage

- CLI: `npm run start:cli`
- Server: `npm run start:server`

